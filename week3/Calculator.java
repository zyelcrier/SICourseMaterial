public class Calculator {
  public static void main(String[] args){
    //TODO: #1 -create a scanner name it anything you want

    boolean calc = true;
    while(calc){
      //TODO: #2 - prompt the user for two numbers set the variables to type double {num1} {num2}

      //TODO: #3 - prompt the user for operation type this will be type string
     //{Multipulcation: * , Addition: + , Subtraction: - , Division: / , Modulous: %}

      //TODO: #4 - check if the user entered any of the right operands to include multiple arguments in one if 
     //use || for or

      //TODO: #5  - if the operand is addtion do the addtion  and save result to variable sum then 
      //output in folowing format {num1 "+" num2 "=" sum} (USE PRINT F)

      //TODO: #6  - if the operand is subtraction do the subtraction  and save result to variable difference
      //output in folowing format {num1 "-" num2 "=" difference} (USE PRINT F)

      //TODO: #7  - if the operand is multipulcation do the multipulcation  and save the result to variable product 
      //output in folowing format {num1 "*" num2 "=" product} (USE PRINT F)

      //TODO: #8  - if the operand is division do the division  and save the result to variable quotient 
      //output in folowing format {num1 "/" num2 "=" quotient} (USE PRINT F)

      //TODO: #9  - if the operand is modulous do the modulous  and save the result to variable remainder 
      //output in folowing format {num1 "%" num2 "=" remainder} (USE PRINT F)

      //TODO: #10 - ask the user if they want to perform another calculation save that to choice{(yes/no)}

      //TODO: #11 - if choice is yes set calc equal to true

      //TODO: #12 - if choice is no or aything else but yes say thank you for using my calculator


    }
  }
}
